## Packages
(none needed, using pre-installed UI components and libraries)

## Notes
- Assume `@shared/schema` exports `tickets`, `insertTicketSchema`, `Ticket`, `InsertTicket`, `UpdateTicketRequest`.
- Assume `@shared/routes` exports `api`, `buildUrl`.
- API endpoints are mounted at `/api/tickets` and `/api/tickets/sync`.
